import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.msdg.mymoneyAPP',
  appName: 'My Money App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
